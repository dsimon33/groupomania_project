export default class Post {
  constructor(title, description, attachment,report,userId) {
    this.title = title;
    this.description = description;
    this.attachment = attachment;
    this.report = report;
    this.userId = userId
  }
}