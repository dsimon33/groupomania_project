export default class User {
  constructor(username, email, password, introduction) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.introduction = introduction
  }
}